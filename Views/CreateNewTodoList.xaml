<Window x:Class="ToDo_NewLogic.Views.CreateNewTodoList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDo_NewLogic.Views"
        mc:Ignorable="d"
        Title="CreateNewTodoList" Height="150" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Enter Todo List Title:" Margin="10" HorizontalAlignment="Center" VerticalAlignment="Center" />

        <TextBox x:Name="TxtTodoListTitle" Grid.Row="1" Margin="10" HorizontalAlignment="Stretch" VerticalAlignment="Center" />

        <Button x:Name="BtnCreateTodoList" Grid.Row="2" Content="Create" Margin="10" HorizontalAlignment="Center" VerticalAlignment="Center" Click="BtnCreateTodoList_Click" />
    </Grid>
</Window>
